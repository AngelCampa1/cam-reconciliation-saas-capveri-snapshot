# Goal: Retired public booking links across CapVeri

This ledger records a superseded effort. Public calendar booking links were removed on 2026-06-29 and must not be treated as active CapVeri behavior.

## Current policy
- Do not expose external calendar booking URLs on the public site.
- Public CTAs route to self-serve trial, pricing, contact, resources, or email.
- AI SDR and AI CS contexts keep `meetingLinks` as an empty compatibility field.

## Superseded architecture decision
The old implementation used generated public CTAs. That path is no longer active.
Generated by `scripts/generate-public-knowledge.mjs` into marketing/frontend/backend/knowledge generated copies.
`cloudflare-backend/src/generated/public-knowledge.json` is NOT auto-written by the generator â€” must be synced manually (`cp knowledge/generated/public-knowledge.json cloudflare-backend/src/generated/public-knowledge.json`). (It was also stale: predated the certificates-removal commit bf4eecb9; sync fixes that too.)

Generated public knowledge should not contain external booking hosts.

## Superseded copy
The old booking copy is no longer approved for public use. Route visitors to the product trial, pricing, contact page, resources, or email.

## Placement plan
Superseded. Do not add public calendar placement without a new decision.

## Superseded implementation history
Earlier work added booking links to CapVeri and Ventora AI workers. That history is intentionally retired. Current implementations may keep a `meetingLinks` field for schema compatibility, but it should be empty for CapVeri unless a new product decision restores public booking links.

## Current regression expectation
- Source and generated public knowledge contain no external booking hosts.
- Marketing and backend AI SDR/AI CS contexts return `meetingLinks: []`.
- Public-site local and production E2E check page HTML, DOM references, and browser requests for external booking hosts.
