"""Performance benchmarks for CapVeri ingestion.

These tests are marked with @pytest.mark.benchmark and can be run with:
    pytest tests/benchmarks/ -v -m benchmark

Or excluded from regular test runs with:
    pytest -m "not benchmark"
"""
